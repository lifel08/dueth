<% @instruments.each do |instrument| %>
  <%= link_to instrument_path(instrument) do %>
    <div class="col sm">
      <div class="image-container">
          <%= cl_image_tag instrument.photo.key, class:'instrument-image', height: 250, width: 250, crop: :fill, loading:"lazy" %>
          <i class="fas fa-map-marker-alt"></i> <p class="location"><%= instrument.location %></p><i class="far fa-heart"></i>
      </div>

        <div class="header-image">
        </div>

      </div>
        <div class="overview">
          <p class="price"> <%= instrument.price %> € /h</p>
          <p><%= image_tag 'svg/rating-grey.svg', class: "rating" %> <%= pluralize(instrument.reviews.count, 'review') %></p>
        </div>
        <p class="span-headline"><%= instrument.title %></p>
      </div>
  <% end %>
<% end %>


